/// Domain error types (`DomainError`).
pub mod error;
/// Domain model types: `TemplateBook`, nodes, IDs, changelog entries.
pub mod model;
/// Persistence-abstraction traits implemented by the `infra` layer.
pub mod repository;
